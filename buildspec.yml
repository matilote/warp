version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
  pre_build:
    commands:
      - apt update
      - apt install -y software-properties-common
      - add-apt-repository -y ppa:deadsnakes/ppa
      - apt update
      - apt install -y python3.7-dev libfmt-dev libz3-dev libcvc4-7 libgmp3-dev libboost-all-dev
  build:
    commands:
      - pip install poetry
      - poetry run make test_semantics
  post_build:
    commands:
      - echo Build completed on `date`
